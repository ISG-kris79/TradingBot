<Window x:Class="TradingBot.BacktestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:TradingBot"
        mc:Ignorable="d"
        Title="Backtest Result" Height="600" Width="1000" WindowStartupLocation="CenterOwner"
        Background="#0F111A" Foreground="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 결과 요약 -->
        <Border Grid.Row="0" Background="#161925" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="Total Profit" Foreground="#94A3B8" FontSize="12"/>
                    <TextBlock Text="{Binding Result.TotalProfit, StringFormat={}{0:N2} USDT}" FontSize="18" FontWeight="Bold" Foreground="{Binding ProfitColor}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="Profit %" Foreground="#94A3B8" FontSize="12"/>
                    <TextBlock Text="{Binding Result.ProfitPercentage, StringFormat={}{0:F2}%}" FontSize="18" FontWeight="Bold" Foreground="{Binding ProfitColor}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="Win Rate" Foreground="#94A3B8" FontSize="12"/>
                    <TextBlock Text="{Binding Result.WinRate, StringFormat={}{0:F1}%}" FontSize="18" FontWeight="Bold" Foreground="#3B82F6"/>
                </StackPanel>

                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock Text="Max Drawdown" Foreground="#94A3B8" FontSize="12"/>
                    <TextBlock Text="{Binding Result.MaxDrawdown, StringFormat={}{0:F2}%}" FontSize="18" FontWeight="Bold" Foreground="#EF4444"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. 차트 -->
        <lvc:CartesianChart Grid.Row="1" Series="{Binding ChartSeries}" LegendLocation="Bottom" Hoverable="True" DataTooltip="{x:Null}">
            <lvc:CartesianChart.ChartLegend>
                <lvc:DefaultLegend Foreground="White"/>
            </lvc:CartesianChart.ChartLegend>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Time" Labels="{Binding Labels}" Foreground="#94A3B8">
                    <lvc:Axis.Separator>
                        <lvc:Separator Stroke="#2D3142" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Price" LabelFormatter="{Binding Formatter}" Foreground="#94A3B8">
                    <lvc:Axis.Separator>
                        <lvc:Separator Stroke="#2D3142" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</Window>